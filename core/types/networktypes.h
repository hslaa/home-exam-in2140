#ifndef networktypes
#define networktypes

typedef struct Graph {
    // Graph
} Graph;

typedef struct Node {
    // Node
} Node;

typedef struct Connection {
    int weight;
    struct Node destination;
} Connection;


#endif
